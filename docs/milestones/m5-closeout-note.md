# M5 closeout gate

This file intentionally records that M5 is not considered complete until the final `feat/m5-appview-portability` pull-request head passes the repository's exact-head `npm run verify` and Docker build checks and is merged into `dev`.

After that merge, update `docs/milestones/m5-atproto.md`, `docs/roadmap.md`, and `docs/README.md` to mark M5 complete and M6 current on a separately tested closeout head.
